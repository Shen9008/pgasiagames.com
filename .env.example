# Blog sync — put values in `.env` and/or `.env.local` (see steps.md).
# Loads `.env` first, then `.env.local` (overrides).
STRAPI_API_URL=http://localhost:1337/api
# STRAPI_API_TOKEN=
# SITE_DOMAIN=pgasiagames.com
# SKIP_POSTS_SITE_FILTER=1
# POSTS_COLLECTION=posts
# POSTS_SITE_FILTER_KEY=filters[site][domain][$eq]
